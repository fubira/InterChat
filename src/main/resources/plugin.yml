name: InterChat
author: fubira
main: net.ironingot.interchat.InterChatPlugin
version: 0.8.1
commands:
  interchat:
    aliases: [ic]
    permissions: interchat.command
    description: InterChat
    usage:
      /<command> 
      /<command> version
      /<command> reload
      /<command> help
